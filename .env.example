# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it may contain secrets.

# Development (local json-server)
# VITE_API_BASE=http://localhost:3001

# Production (your deployed backend URL, e.g. Railway / Render / Fly.io)
# VITE_API_BASE=https://your-backend.up.railway.app
