{
  "name": "ncert-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for NCERT Preparation Register — serves and persists progress data via REST API.",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": ["json-server", "ncert", "upsc", "api"],
  "author": "",
  "license": "MIT"
}
